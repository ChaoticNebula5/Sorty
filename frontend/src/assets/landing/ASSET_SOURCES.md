# Landing Page Asset Sources

The images used in the landing page are placeholder/reference images copied from the internal `_reference/v0-sorty/public/events` directory.

No external assets were downloaded for Stage 1.
